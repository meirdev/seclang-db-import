[project]
name = "seclang-db-import"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "msc-pyparser>=1.2.2",
]

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]

[tool.ruff.lint]
extend-select = ["I"]

[dependency-groups]
dev = [
    "ruff>=0.15.11",
]
